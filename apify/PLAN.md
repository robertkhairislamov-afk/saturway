# План: пассивный доход через Apify Store

## Цель

Стабильно получать не меньше $100 в месяц чистыми без ручной работы с клиентами.

## Что уже решено

- **Saturway отложен.** Шанс заработать на нём оценён меньше чем в 1%. Бэкенд не запускается: 45 ошибок TypeScript, Zod-схемы переданы в Fastify без type provider. Фронтенд — макет без API.
- **Рынок России исключён.**
- **Модель — портфель примерно из 10 акторов в Apify Store** с оплатой за события (pay-per-event).
  - Apify платит 80% выручки минус стоимость запусков.
  - Выплаты раз в месяц: на PayPal от $20, на банковский счёт от $100.
  - Чтобы получать $100 чистыми, нужно около $150 выручки в месяц на все акторы вместе.
- **Логика вероятностей.** Шанс, что хотя бы один актор принесёт $100 в месяц, равен 1 − (1 − p)ⁿ.
  - При p ≈ 8% и 10 акторах это около 57%.
  - Хороший выбор ниш поднимает p до 10–12%, и шанс вырастает до 65–72%.
- **Инструмент поиска ниш — внутренний, не продаём.** В Store уже есть минимум шесть аналогов, например `johnvc/store-actor-intelligence-api` и `groundworklabs/apify-opportunity-finder`.

## Анализ ниш — сделан

Результаты — в [NICHES.md](NICHES.md), скрипты — в `niche-finder/`. Главное:

- $100 в месяц — это примерно 40 пользователей в месяц на все акторы вместе, при средней выплате около $2,5 за пользователя.
- По данным магазина, из разработчиков с 10–19 акторами до этого уровня доходит около половины, с одним актором — 5%.
- Первые 10 акторов: Google News, Google Play, Greenhouse, Workday, Lever, Ashby, Dice, Welcome to the Jungle, Rightmove, Home Depot.

## Первая волна акторов

Собрать и опубликовать акторы 1–6 из NICHES.md: Google News, Google Play, Greenhouse, Workday, Lever, Ashby. У вакансионных акторов общая основа кода и одинаковый формат результата.

### 1. Google News — опубликован

Код лежит в `actors/google-news/`. Прошли 11 тестов и живые запуски: поиск, темы, топ-новости, немецкое издание, диапазон дат по дням, лимиты, расшифровка ссылок на издания.

На Apify: актор `robertosan16/google-news-scraper` (ID `pyiyAg3vjflJK3zZC`), сборка 0.1.1.

- Пробный запуск с Apify Proxy: 60 статей, расшифрованы 60 ссылок из 60, 83 секунды, расход $0.0009.
- Название, описание, категории (NEWS, MARKETING, AI) и SEO заданы через API.
- Платёжные данные для выплат заполнены: компания и PayPal.
- Цены заданы через API 24.09.2026: $0.002 за статью (`apify-default-dataset-item`), $0.001 за расшифрованную ссылку (`url-resolved`), без платы за старт.
- Опубликован в Store 24.09.2026: https://apify.com/robertosan16/google-news-scraper. Действующая сборка 0.1.3: при пустом вводе запускается пример поиска, чтобы проходил ежедневный автотест Apify.
- Память по умолчанию снижена с 4096 до 512 МБ 24.09.2026: расход платформы на запуски вычитается из дохода разработчика. Проверочный запуск с расшифровкой ссылок прошёл за 28 секунд.

Как опубликовать:

1. Выложить в Apify. Два способа:
   - через API с токеном из API credentials окружения: создать актор из `actors/google-news` и собрать;
   - через Apify Console: Link Git repository → ветка `claude/gifted-dirac-sv0ed6`, папка `apify/actors/google-news`.
2. Проверить запуск на платформе с Apify Proxy.
3. В Console в разделе Monetization выбрать pay-per-event:
   - `apify-default-dataset-item` — $0.002 за статью;
   - `url-resolved` — $0.001 за расшифрованную ссылку;
   - без платы за старт.
4. Указать PayPal для выплат и опубликовать в Store.

### 2. Семейство акторов с вакансиями — опубликовано

Четыре актора на одном общем коде. Код лежит в `actors/ats-jobs/`. Тесты общие для всех акторов на этой основе, все проходят.

| Актор | ID | Пример на пустом вводе |
|---|---|---|
| `robertosan16/greenhouse-jobs-scraper` | `CF2hrHKzxtxlpByW5` | airbnb: 160 вакансий, 3 с |
| `robertosan16/lever-jobs-scraper` | `LihMgbNe42YWb2Llh` | zoox: 236 вакансий, 4 с |
| `robertosan16/ashby-jobs-scraper` | `Cyncc8yUcPe5JFx3k` | ramp: 155 вакансий, 3 с |
| `robertosan16/workday-jobs-scraper` | `UoHnx8rozYHpGQURS` | NVIDIA: 50 вакансий, 9 с |

- Цена: $0.001 за вакансию (`apify-default-dataset-item`), без платы за старт. Задана через API 24.09.2026.
- Название, описание, категории (JOBS, LEAD_GENERATION, AUTOMATION), SEO и память 512 МБ заданы через API. Действующая сборка 0.1.4.
- Прогоны на платформе: пустой ввод и примеры из формы проходят, расход меньше $0.002 за прогон.
- Опубликованы в Store 24.09.2026: страницы `https://apify.com/robertosan16/<имя актора>` открываются. В поиске Store новые акторы появляются с задержкой: API магазина уже считает их, но пока не показывает в выдаче.

Что умеют:

1. **Единый формат** для всех четырёх систем: должность, отдел, команда, все локации, удалёнка, тип занятости, зарплата `{min, max, currency, interval, source}`, описание текстом и HTML, ссылки на вакансию и отклик, даты.
2. **Зарплаты.**
   - Из данных самой системы: у Greenhouse — через `pay_transparency`, у Lever — `salaryRange`, у Ashby — compensation.
   - Из текста описания: например, «168,000 USD - 264,500 USD». Проверено на реальных данных, ложных срабатываний на суммах вроде «рынок $100B» нет.
   - Результаты: у NVIDIA зарплата найдена во всех вакансиях в США, у Airbnb — в 134 вакансиях из 160.
3. **Workday больше 2000.** Поиск Workday отдаёт максимум 2000 вакансий. Актор делит поиск по фильтрам самого сайта, например по категориям, и собирает всё: у NVIDIA 2655 вакансий. Страницы результатов грузятся параллельно.
4. **Фильтры:**
   - ключевые слова в названии, с производными формами (engineer → Engineering) и `*`;
   - исключения, локации, только удалённые, отдел, дата публикации, только с зарплатой.
   - У Workday ключевые слова ищутся и на самом сайте.
5. **«Только новые с прошлого запуска».** Виденные вакансии хранятся в именованном хранилище пользователя отдельно для каждой задачи и компании. Карточки Workday загружаются только для новых вакансий.
6. **Ввод:** названия, любые ссылки на доски, для Workday — ссылка на страницу поиска вместе с выбранными на сайте фильтрами.

Как обновить акторы:

1. `cd actors/ats-jobs && npm test`.
2. `node scripts/build.mjs --out /tmp/ats-actors` — собирать вне репозитория, иначе `apify push` пропускает файлы из `.gitignore`.
3. `cd /tmp/ats-actors/<ats> && apify push`.

Идеи на потом:

- Режим «поиск по всем компаниям» со встроенным списком досок. Так работают лидеры ниши fantastic-jobs: они ищут вакансии сразу по всем компаниям, а не по списку.
- Отдел для Workday через фильтр «Job Category» сайта.

Теперь в портфеле 5 акторов. По данным магазина, до $100 в месяц доходят около 39% разработчиков с 5–9 акторами (верхняя граница), против ~5% с одним.

### 3. Google Play — выложен, ждёт публикации

Код лежит в `actors/google-play/`, тестов 7, все проходят. На Apify: актор `robertosan16/google-play-scraper` (ID `FPgPnid4ZggrEJEaY`).

- Цена: $0.0005 за результат ($0.50 за 1000), без платы за старт. Конкуренты: solidcode — $0.5 за 1000 плюс $0.01 за старт, curious_coder — $0.3 за 1000, automation-lab — $2.3 за 1000, у него 22% запусков падают.
- Категории: DEVELOPER_TOOLS, SEO_TOOLS, LEAD_GENERATION. Память 512 МБ.
- Отзывы не собираем, как и решили. Адреса и телефоны разработчиков тоже не берём: у частных разработчиков это персональные данные. Email поддержки и сайт берём — их публикует сам Google Play.

Что умеет:

1. Топ-чарты: бесплатные, платные, самые кассовые. Любая из 54 категорий, до 200 мест, любая страна, место каждого приложения.
2. Поиск по ключевым словам с позициями — до 30 приложений, как в самом магазине. Нужен для отслеживания позиций (ASO).
3. Карточка приложения: рейтинг, число оценок, распределение 1–5 звёзд, установки (включая точную оценку Google), цена и скидка, покупки внутри приложения, реклама, версия, даты, контакты, описание, скриншоты.
4. Все приложения разработчика: по имени, числовому ID или ссылке.
5. Несколько стран за один запуск, 33 языка. Быстрый режим без карточек: тысячи мест в чартах за секунды.

Технически:

- Карточки и поиск читаются из данных страницы (`AF_initDataCallback`).
- Чарты и следующие страницы каталога разработчика — через `batchexecute` (`vyAe2`, `qnKhOb`). Формат запроса чартов взят из открытой библиотеки google-play-scraper 10.1.3.
- Подключение по умолчанию прямое: через Apify Proxy карточки грузились в 10 раз медленнее. Если Google ограничит запросы (429 или страница «sorry»), актор сам переключится на Apify Proxy.

Проверка на платформе, сборка 0.1.3:

- пустой ввод — 50 приложений с карточками за 4–18 секунд;
- пример из формы — 80 приложений за 22 секунды;
- большой прогон: 2 чарта × 2 категории × 3 страны = 2400 приложений с карточками за 11 минут, расход платформы $0.03 при выручке $1.20.

Около 1% карточек иногда приходит без данных, поэтому актор перезапрашивает такие страницы.

### 4. Dice — выложен, ждёт публикации

Пятый актор на общей основе вакансионных: код в `actors/ats-jobs/src/adapters/dice.js`, тесты — в общем наборе `actors/ats-jobs`. На Apify: `robertosan16/dice-jobs-scraper` (ID `Nz04h9AaeYNbrMKRd`), сборка 0.1.2.

- Цена: $0.0005 за вакансию ($0.50 за 1000), без платы за старт. Лидер shahidirfan (174 пользователя) берёт $1 за 1000 и $0.005 за старт, solidcode — $0.8 за 1000, worldunboxer — $0.1 за 1000 и $0.06 за старт.
- Поиск как на сайте: ключевые слова, город и радиус, все фильтры Dice — удалёнка, тип занятости, дата, работодатель или рекрутер, Easy Apply, визовая поддержка. Можно вставить готовую ссылку поиска с dice.com.
- Данные берутся из страниц Next.js без ключей API: по 30 вакансий на страницу, не больше 25 страниц (750 вакансий). Большие поиски делятся по типу работодателя, затем по типу занятости. По запросу «python developer» собрано 946 вакансий из 951.
- Карточка вакансии: полное описание (schema.org JobPosting), навыки, срок действия, зарплата. Зарплата «$$68 - $68.25 per hour» разбирается в числа.
- Email и телефоны рекрутеров есть в данных страницы, но актор их не читает.
- Формат результата общий с Greenhouse, Lever, Ashby и Workday, плюс поля `skills`, `easyApply`, `employerType`, `validThrough`.
- Подключение прямое. Если Dice начнёт ограничивать запросы (429 или 403), актор переключится на Apify Proxy. Этот переход добавлен в общую основу, но включён только у Dice.

Проверка на платформе:

- пустой ввод — 50 вакансий за 10 секунд;
- «python developer» — 946 вакансий с карточками за 2,5 минуты, расход платформы $0.007 при выручке $0.47;
- два поиска с фильтрами — 60 вакансий за 17 секунд.

### 5. Welcome to the Jungle — выложен, ждёт публикации

Шестой актор на общей основе: код в `actors/ats-jobs/src/adapters/wttj.js`, тесты — в общем наборе (34 теста на 25.09). На Apify: `robertosan16/welcome-to-the-jungle-jobs-scraper` (ID `dqrYgGSWpQABGpxvb`).

- Цена: $0.0005 за вакансию ($0.50 за 1000), без платы за старт. Конкуренты: лидер clearpath (156 пользователей за 30 дней) — $2.99 за 1000, shahidirfan (54, рейтинг 4.6) — $0.99 за 1000 и $0.0005 за старт, logiover (31) — $2.5 за 1000, blackfalcondata — $0.45 за 1000 и $0.005 за старт. Когда появятся отзывы, можно поднять цену до $1 за 1000.
- Данные берутся из открытого API сайта `api.welcometothejungle.com/api/v3`, без ключей:
  - поиск `/public/jobs` — по 10 вакансий на страницу, не больше 10 страниц (100 вакансий на поиск). Фильтры: страны, тип договора, опыт, минимальная зарплата. Фильтр удалёнки сервер не применяет, поэтому актор проверяет его сам;
  - все вакансии компании — `/organizations/<компания>/jobs`;
  - карточка вакансии — описание, ключевые задачи, профиль, навыки, инструменты, опыт, образование, длительность договора, дата начала.
- Большие поиски делятся по странам (если их выбрано несколько), затем по типу договора, затем по опыту. По «product manager» в FR, GB и US собрано 915 вакансий из 2800. Уровни опыта пересекаются, а стажировки попадают во все уровни, поэтому сайт отдаёт не всё. В логе актор советует добавить страны или уточнить запрос.
- На сайте много вакансий не только во Франции: по запросу «engineer» в США 6048, во Франции 4607, в Великобритании 1319.
- Зарплаты компании вводят вручную, и около 4% с ошибкой в единицах. Актор исправляет явные случаи: «34–48 в год» → 34 000–48 000, месячная сумма с пометкой «в год» → в месяц, годовая с пометкой «в месяц» или «в день» → в год. Правило общее (`fixStatedUnits` в `salary.js`). Фильтр минимальной зарплаты, как и сайт, считает месячную зарплату ×12.
- Стажировки и обучение с работой (alternance, apprenticeship) теперь разделены в `employmentType` для всех акторов на общей основе.
- Персональных данных в API нет: только вакансии и компании.

Проверка на платформе, сборки 0.1.1–0.1.3 (действующая 0.1.3):

- пустой ввод — 50 вакансий за 8–10 секунд;
- пример из README (2 запроса, FR и BE, фильтры) — 35 вакансий за 23 секунды;
- две компании с ключевым словом — 29 вакансий за 4 секунды;
- расход платформы — меньше $0.0004 за прогон.

### Публикации

Apify разрешает не больше 5 публикаций в сутки. 24.09.2026 опубликованы Google News и четыре актора ATS. Google Play, Dice и Welcome to the Jungle ждут публикации: сначала Apify должен подтвердить компанию (ожидается около 29.09.2026). До подтверждения опубликованные акторы не видны в поиске Store.

После публикации:

1. Добавить ссылки на Dice и Welcome to the Jungle в README четырёх акторов ATS и пересобрать все шесть. Общая основа менялась 25.09 (alternance, тексты сообщений), так что пересборка заодно обновит старые акторы.
2. Проверить, что новые акторы видны в поиске Store, и их позиции.

### 6. Потом

Rightmove, Home Depot.

## Как был сделан анализ (для пересчёта)

1. Собрать данные через публичный Store API без токена:
   `GET https://api.apify.com/v2/store?limit=1000&offset=N` с постраничным обходом.
   Нужные поля: `title`, `name`, `username`, `categories`, `stats.totalUsers`, `stats.totalUsers7Days`, `stats.totalUsers30Days`, `stats.totalUsers90Days`, `stats.lastRunStartedAt`, `currentPricingInfo`. Перед написанием кода проверить реальный формат ответа.
2. Сгруппировать акторы по целевому сайту или платформе, определяя их по `title` и `name`.
3. Оценить каждую нишу: оценка = спрос × слабость конкурентов × простота сбора данных.
   - Спрос — сумма `totalUsers30Days` по группе.
   - Слабость конкурентов — мало акторов, у лидера небольшая доля, лидеры давно не обновлялись или плохо оценены.
   - Простота сбора — насколько сайт защищается от парсинга. Оценить вручную для лучших кандидатов.
4. Выдать топ-10 ниш с обоснованием.

## После анализа

- Собирать и публиковать по актору каждые 1–2 дня: JS/TS, Crawlee, Apify SDK.
- Брать только публичные данные без персональной информации и соблюдать правила сайтов.
- Через 4–6 недель пересмотреть портфель:
  - есть пользователи, но нет денег — менять цену;
  - нет пользователей — менять нишу или карточку актора.
  - Слабые акторы снимать, сильные улучшать.

## Доступы

- В облачном окружении включён полный сетевой доступ (Full).
- Токен Apify для публикации добавлять через **API credentials** в настройках окружения, а не в переменные окружения: переменные видны всем, кто пользуется окружением.
