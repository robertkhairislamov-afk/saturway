# Project Validator - Валидатор Проекта

**Описание:** Агент для проверки целостности и правильности выполнения задач в проекте Saturway.

## Задачи агента

1. **Проверка структуры проекта**
   - Все необходимые файлы на месте
   - Правильная структура папок
   - Наличие конфигурационных файлов

2. **Валидация кода**
   - TypeScript типы корректны
   - Нет конфликтов импортов
   - Все компоненты экспортируются правильно

3. **Проверка зависимостей**
   - Все пакеты установлены
   - Версии совместимы
   - Нет security уязвимостей

4. **Тестирование сборки**
   - Frontend собирается без ошибок
   - Backend запускается корректно
   - Нет lint ошибок

## Как использовать

Запусти этот skill когда:
- Завершил большую задачу
- Перед коммитом в git
- При появлении странных ошибок
- Перед деплоем

## Что проверяет

```bash
# 1. Структура проекта
saturway-app/
  ├── src/
  │   ├── components/
  │   ├── store.ts
  │   ├── App.tsx
  │   └── main.tsx
  ├── package.json
  └── vite.config.ts

saturway-backend/
  ├── src/
  ├── package.json
  └── .env.example

# 2. Frontend checks
cd saturway-app
npm install
npm run lint
npm run build

# 3. Backend checks
cd saturway-backend
npm install
npm run lint (если есть)

# 4. TypeScript проверка
tsc --noEmit (в обоих проектах)
```

## Действия при ошибках

1. **Ошибки импортов:**
   - Проверь все import/export
   - Убедись что файлы существуют
   - Проверь расширения (.ts, .tsx)

2. **TypeScript ошибки:**
   - Проверь типы всех props
   - Убедись что интерфейсы совпадают
   - Добавь недостающие типы

3. **Build ошибки:**
   - Очисти node_modules и переустанови
   - Проверь конфликты версий
   - Убедись что все зависимости в package.json

4. **Lint ошибки:**
   - Запусти автофикс: npm run lint --fix
   - Исправь оставшиеся вручную

## Отчет

После проверки предоставь:
- ✅ Что работает
- ❌ Что сломано
- 🔧 Как исправить
- 📊 Общая оценка готовности проекта
